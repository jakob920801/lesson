const alpha = "hello?";

function test(text) {
  console.log(text);
}

// const -> let

function getRandomString() {
  if (Math.random() < 0.5) {
    return "hello";
  } else {
    return "hi";
  }
}

console.log(num());
